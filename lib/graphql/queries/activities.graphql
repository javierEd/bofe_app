query Activities($after: UUID, $first: Int, $includeGlobal: Boolean! = true) {
    activities(after: $after, first: $first, includeGlobal: $includeGlobal) {
        nodes {
            ...ActivityFragment
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
