query ListCards($id: ID!, $after: UUID, $first: Int) {
    list(id: $id) {
        id
        cards(after: $after, first: $first) {
            nodes {
                ...CardFragment
            }
            pageInfo {
                endCursor
            }
        }
        createdAt
        updatedAt
    }
}
