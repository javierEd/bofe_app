query CurrentUserBoards($after: UUID, $first: Int) {
    currentUser {
        id
        boards(after: $after, first: $first) {
            nodes {
                ...BoardFragment
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }

        createdAt
        updatedAt
    }
}
