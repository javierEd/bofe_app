name: Setup Flutter
description: Setup Flutter environment with dependencies

runs:
    using: composite
    steps:
        - uses: subosito/flutter-action@v2
          with:
              channel: stable
              cache: true
        - name: Install dependencies
          shell: bash
          run: flutter pub get
        - name: Build with build_runner
          shell: bash
          run: dart run build_runner build
